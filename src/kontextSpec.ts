describe("Kontext specs", () => {
  it("sets instance by passing a factory");
  it("gets new instance");
  it("gets single instance when asSingleton() is called");
  it("sets request and handlers");
  it("gets request and allows to use callback");
})
