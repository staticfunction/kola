/**
 * Created by staticfunction on 8/27/14.
 */
describe("Kontext factory storage test", () => {
    it('can store a factory that returns new instances', () => {

    });

    it('can store a factory that returns only one instance', () => {

    });

    it('returns a factory from the parent if current kontext does not have it', () => {

    });

    it('throws an error if factory is not defined', () => {

    })
});